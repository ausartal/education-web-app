# Fix KPS Exam — Comprehensive Fix Plan

## Issues to Fix

### 1. Firestore undefined values (ROOT CAUSE of 500 error)
**File:** `src/app/api/kps/sessions/[id]/stage/route.ts`
**Problem:** `KPSQuestionResponse` has fields like `selectedAnswer`, `selectedAnswers`, `booleanAnswer`, etc. When a question is MC, `selectedAnswers` is `undefined`. Firestore rejects `undefined`.
**Fix:** Strip all `undefined` fields from the response object before saving.

### 2. Results only viewable by admin (NEW REQUIREMENT)
**Files:**
- `src/app/api/kps/sessions/[id]/route.ts` — filter response data for non-admin
- `src/app/(kps-ujian)/ujian-kps/[sessionId]/results/page.tsx` — show "menunggu penilaian admin" for students
- `src/app/(kps-ujian)/ujian-kps/scores/page.tsx` — students see their scores but not full breakdown
- `src/app/api/kps/scores/route.ts` — filter for students
- `src/app/api/kps/scores/[sessionId]/route.ts` — filter for students

**Change:** Students can see their own score/level but NOT detailed per-question breakdown or indicator scores. Only admin sees full detail.

### 3. Questions randomized per session
**File:** `src/app/api/kps/sessions/start/route.ts`
**Current:** Picks one random stimulus per level.
**Change:** For each session, randomly pick a stimulus. Already done, but verify it works.

### 4. MSAT adaptive flow verification
**Files:**
- `src/lib/kps-engine.ts` — verify branching logic
- `src/app/api/kps/sessions/[id]/stage/route.ts` — verify path determination
**Verify:**
- Stage 1: menengah → ≥5/7 → tinggi path, <5 → rendah path
- Stage 2: tinggi path + ≥5/7 → stage 3 tinggi, etc.
- Stage 3: final level determined correctly

### 5. Scoring and indicator scores
**File:** `src/app/api/kps/sessions/[id]/complete/route.ts`
**Verify:**
- `calculateNumericScore` weights: S1=20%, S2=30%, S3=50%
- `calculateIndicatorScores` groups by indicator across all stages
- `determineFinalLevel` uses path combination + score refinement

## Implementation Steps

### Step 1: Fix undefined values in stage route
```typescript
// Helper to strip undefined values from an object
function stripUndefined(obj: Record<string, unknown>): Record<string, unknown> {
  const result: Record<string, unknown> = {};
  for (const [key, value] of Object.entries(obj)) {
    if (value !== undefined) {
      result[key] = value;
    }
  }
  return result;
}
```
Apply to each `KPSQuestionResponse` before adding to `scoredResponses`.

### Step 2: Update results visibility
- Students see: score, level, date, testId
- Students do NOT see: per-question correctness, indicator breakdown, stage details
- Admin sees: everything

### Step 3: Verify adaptive engine
- Check `getNextStagePath` returns correct paths
- Check `getStage3Level` maps correctly
- Check `determineFinalLevel` with score refinement

### Step 4: Verify scoring
- MC: 1.0 if correct, 0.0 if wrong
- Complex MC: partial credit formula
- T/F: 1.0 if correct
- Complex TF: fraction correct or all-or-nothing
- Matching: fraction correct pairs

### Step 5: Test flow
- Start with code KPS2026
- Answer 7 questions → submit → should succeed
- Break screen → skip
- Answer 7 more → submit → should succeed
- Break screen → skip
- Answer 7 more → submit → should succeed
- Results page shows score/level (student view)
- Admin can see full detail

## Files to Modify
1. `src/app/api/kps/sessions/[id]/stage/route.ts` — strip undefined
2. `src/app/api/kps/sessions/[id]/route.ts` — filter results for student
3. `src/app/(kps-ujian)/ujian-kps/[sessionId]/results/page.tsx` — student view
4. `src/app/api/kps/scores/route.ts` — filter for student
5. `src/app/api/kps/scores/[sessionId]/route.ts` — filter for student

## Verification
1. `npx tsc --noEmit` — no TypeScript errors
2. Manual test: full exam flow
3. Check Firestore: no undefined values in documents
4. Check admin can see full results
5. Check student sees limited results
