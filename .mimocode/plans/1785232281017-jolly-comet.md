# Perbesar Modal Tambah Soal

## Change
File: `src/app/(dashboard)/teacher/bank-soal/page.tsx` line 1025

Change `max-w-2xl` → `max-w-4xl` (672px → 896px)

```
- ${wide ? 'max-w-2xl max-h-[90vh] overflow-y-auto' : 'max-w-md'}
+ ${wide ? 'max-w-4xl max-h-[90vh] overflow-y-auto' : 'max-w-md'}
```

## Verification
- `npx tsc --noEmit` — clean
- Visual check on localhost
